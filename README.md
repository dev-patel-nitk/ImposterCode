
  # Select Current Item

  This is a code bundle for Select Current Item. The original project is available at https://www.figma.com/design/jngqCpTOULGdPhAzEX0UXB/Select-Current-Item.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  